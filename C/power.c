#include <stdio.h>
int main()
{
    int a;
    printf("Enter any integer : ");
    scanf("%d", &a);
    printf("\nSquare of the number : %d", (a*a));
    printf("\nCube of the number : %d", (a*a*a));
    return 0;
}